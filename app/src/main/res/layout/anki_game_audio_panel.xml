<?xml version="1.0" encoding="utf-8"?>
<!-- Inline game-audio editing panel inside the sentence card's Original
     group: the trim waveform, nothing else — playback lives on the row's
     preview chip above, and the save-time editor covers never-reviewed
     audio. Shown only while the sentence audio source is the game
     recording. 24dp horizontal padding keeps handles clear of the system
     back-gesture zones even inside the sheet. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingBottom="10dp">

    <com.playtranslate.ui.WaveformTrimView
        android:id="@+id/waveInline"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_marginTop="4dp" />

</LinearLayout>
