<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="screenshots" path="screenshots/" />
    <cache-path name="logs" path="logs/" />
    <cache-path name="tts" path="tts-audio/" />
    <!-- AudioCache clips (game-audio m4a, Commons recordings) attached to
         Anki cards via AnkiManager.addMediaFromFile. -->
    <cache-path name="audioCache" path="audioCache/" />
    <!-- Downloaded self-update APKs, read by the system package installer. -->
    <cache-path name="updates" path="updates/" />
    <files-path name="crashes" path="crashes/" />
</paths>
